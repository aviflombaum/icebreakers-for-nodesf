<!DOCTYPE html>
<html>
  <% include ../layout/header.ejs %>
  <body>
    <div class="container">
      <% include ../layout/navigation.ejs %>

      <main role="main">  
        <div class="row marketing">
          <div class="col-lg-12">
            <div class="card">
              <h4 class="card-header">You've been invited to answer an Icebreaker!</h4>
              <div class="card-body">
                
                <p><h3><%= question.content %></h3></p>

                <form action="/responses?secret=<%= iceBreakerResponse.secret %>" method="POST">
                  <div class="form-group">
                    <textarea class="form-control" name="responseText" rows="3" placeholder="Type your response here..."></textarea>
                  </div>

                  <button type="submit" class="btn btn-primary">Submit Your Answer and See Other People's Responses!</button>
               </form>
              </div>
            </div>
          </div>
        </div>
      </main>
      
      <% include ../layout/footer.ejs %>
    </div> <!-- /container -->
  </body>
</html>