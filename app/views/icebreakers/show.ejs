<!DOCTYPE html>
<html>
  <% include ../layout/header.ejs %>
  <body>
    <div class="container">
      <% include ../layout/navigation.ejs %>

      <main role="main">
        <div class="row marketing">
          <div class="col-lg-12">
            <div class="card">
              <h4 class="card-header">Your Icebreaker</h4>
              <div class="card-body">
                
                <p class="card-text">Each person gets their own secret link to answer the question before they can see anyone else's answer. You can txt them or email them the link if you want.</p>
                
                <p class="card-text">
                  The link for your Icebreaker is also secret, so make sure to copy it or write it down: 

                  <div class="alert alert-info" role="alert">
                    <%= icebreakerURL %>
                  </div>

                  Don't share this, it's just for you.
                </p>
                <h4 class="card-title">Question: <%= question.content %></h4>
                <h4>Invitees:</h4>
                <ul class="list-group list-group-flush">
                <% iceBreakerResponses.forEach(function(iceBreakerResponse){ %>
                  <li class="list-group-item">
                    <h5><%= iceBreakerResponse.email %></h5>
                    <p>Their Secret Response Link: <a href="<%= siteURL + "/responses/edit?secret=" + iceBreakerResponse.secret %>"><%= siteURL + "/responses/edit?secret=" + iceBreakerResponse.secret %></a></p>
                    <p><%= iceBreakerResponse.responseText %></p>
                  </li>
                <% }) %>
                </ul>                
              </div>
            </div>
          </div>
        </div>
      </main>

      <% include ../layout/footer.ejs %>
    </div> <!-- /container -->
  </body>
</html>
